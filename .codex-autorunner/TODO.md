# TODO

- [ ] Build the V2 web UI (Dashboard/Docs/Logs/Chat) talking to the API; ship as static assets served by `serve`.
- [ ] Add SSE/WebSocket streaming to chat and logs views in the UI.
- [ ] Support optional auth token enforcement in the UI and document usage.
- [ ] Expose kill/resume controls in the UI using new API endpoints.
- [ ] Write a README with quickstart instructions for running codex-autorunner locally.
- [ ] Add a smoke test covering config loading and prompt construction.
- [ ] Add CI or a simple check script to run formatting/tests.
